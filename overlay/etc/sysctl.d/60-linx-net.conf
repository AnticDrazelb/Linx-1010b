# Network tuning. The Wi-Fi is a low-end SDIO part; fq_codel keeps latency sane
# when the link saturates, which it does easily at 802.11n speeds.
net.core.default_qdisc = fq_codel
net.ipv4.tcp_congestion_control = bbr
net.ipv4.tcp_fastopen = 3
